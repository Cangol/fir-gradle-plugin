group = POM_GROUP_ID
version = POM_VERSION

// publish plugin to Gradle Plugin Portal
// https://docs.gradle.org/7.4.2/userguide/publishing_gradle_plugins.html
pluginBundle {
    website = POM_URL
    vcsUrl = POM_SCM_URL
    tags = ['android','gradle','fir']
}

publishing {
    //
}
